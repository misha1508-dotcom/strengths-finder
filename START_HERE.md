# 🚀 Как запустить систему аналитики

## Проблема которую вы испытали

Telegram-бот был запущен, но Next.js сервер НЕ был запущен, поэтому бот не мог получить данные из API.

## Решение: Запустите ОБА сервиса

### Шаг 1: Запустите Next.js (обязательно!)

Откройте **первый терминал**:

```bash
cd "/Users/mihailmirosnicenko/Desktop/vibe projects/inversion"
npm run dev
```

Дождитесь сообщения:
```
✓ Ready in 2.3s
○ Local: http://localhost:3000
```

**НЕ ЗАКРЫВАЙТЕ этот терминал!** Оставьте его работать.

### Шаг 2: Telegram-бот (уже запущен ✅)

Telegram-бот уже запущен и работает в фоне.

Если вы его остановили, запустите во **втором терминале**:

```bash
cd "/Users/mihailmirosnicenko/Desktop/vibe projects/inversion"
node telegram-bot.js
```

## Проверка что всё работает

### 1. Откройте сайт
```
http://localhost:3000
```

### 2. Пройдите весь путь:
- Нажмите "Начать"
- Выпишите минимум 2 ситуации
- Нажмите "Перейти к анализу"
- Дождитесь завершения анализа
- Нажмите "Узнать пёрышки"
- Нажмите "Скопировать данные"

### 3. Проверьте Telegram
Вам должно прийти уведомление:
```
🎯 Новый анализ!

📝 Количество ситуаций: 2
🔑 Session ID: session_...
⏰ Время: ...
```

### 4. Используйте команды бота
```
/stats - общая статистика
/funnel - воронка конверсии
/sessions - последние сессии
```

## Что делать если не работает

### Ошибка "Next.js сервер не запущен"
→ Запустите `npm run dev` в первом терминале

### Ошибка "Нет данных"
→ Откройте http://localhost:3000 и пройдите весь путь пользователя

### Бот не отвечает
→ Проверьте что бот запущен: `node telegram-bot.js`

### Нет уведомлений о новом анализе
→ Убедитесь что ОБА сервиса запущены и пройдите анализ заново

## Быстрый тест без браузера

Если хотите просто проверить что файл с данными создаётся:

```bash
node test-analytics-local.js
```

Это покажет статистику из файла (если он есть).

## Структура проекта

```
ДВА СЕРВИСА ДОЛЖНЫ РАБОТАТЬ ОДНОВРЕМЕННО:

┌─────────────────────────────────────┐
│  ТЕРМИНАЛ 1: npm run dev            │
│  ↓                                  │
│  Next.js сервер на :3000            │
│  ↓                                  │
│  API /api/analytics                 │
│  ↓                                  │
│  Сохраняет в analytics-data.json    │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  ТЕРМИНАЛ 2: node telegram-bot.js   │
│  ↓                                  │
│  Telegram бот                       │
│  ↓                                  │
│  Запрашивает GET /api/analytics     │
│  ↓                                  │
│  Показывает статистику              │
└─────────────────────────────────────┘
```

## Важно!

**ВСЕГДА запускайте ОБА сервиса одновременно:**
1. Next.js (`npm run dev`) - для сбора данных
2. Telegram-бот (`node telegram-bot.js`) - для просмотра статистики

Без Next.js бот НЕ МОЖЕТ получить данные!

---

Удачи! 🚀

Если возникнут вопросы - см. [README_ANALYTICS.md](./README_ANALYTICS.md)
