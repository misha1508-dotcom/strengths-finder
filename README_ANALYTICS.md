# 📊 Система аналитики проекта Inversion

## ✅ Всё настроено и готово к работе!

### Что уже сделано:

1. ✅ Создана система трекинга событий
2. ✅ Настроен API для сбора аналитики
3. ✅ Создан Telegram-бот для уведомлений
4. ✅ Настроены переменные окружения
5. ✅ Установлены все зависимости

### Ваши данные:

- **Bot Token**: `your-telegram-bot-token`
- **Chat ID**: `your-chat-id` (@krechet_mike)
- **Analytics Key**: `your-analytics-secret-key`

---

## 🚀 Как запустить

### Вариант 1: Оба сервиса одновременно

Откройте **два терминала**:

**Терминал 1** - Next.js приложение:
```bash
cd "/Users/mihailmirosnicenko/Desktop/vibe projects/inversion"
npm run dev
```

**Терминал 2** - Telegram-бот:
```bash
cd "/Users/mihailmirosnicenko/Desktop/vibe projects/inversion"
node telegram-bot.js
```

### Вариант 2: Только веб-приложение (без бота)

Если вам нужно только веб-приложение без Telegram-бота:
```bash
cd "/Users/mihailmirosnicenko/Desktop/vibe projects/inversion"
npm run dev
```

Аналитика будет собираться, но уведомления в Telegram не будут отправляться.

---

## 📱 Команды Telegram-бота

После запуска бота откройте чат с ним и используйте команды:

- `/start` - показать список команд
- `/stats` - общая статистика (уники, сессии, медианы)
- `/funnel` - воронка конверсии по этапам
- `/sessions` - последние 10 сессий

### Автоматические уведомления

Бот **автоматически** отправит вам сообщение когда:
- Кто-то завершит анализ ситуаций

Уведомление содержит:
```
🎯 Новый анализ!

📝 Количество ситуаций: 3
🔑 Session ID: session_123_abc
⏰ Время: 18.01.2026, 12:34:56
```

---

## 📊 Что отслеживается

### События пользователя:

1. **page_view** - зашёл на сайт (уники)
2. **situation_started** - начал выписывать ситуации
   _Конверсия: начали / зашли_

3. **situation_saved** - сохранил ситуацию
   _Конверсия: сохранили / начали_
   _Данные: количество символов, индекс ситуации_

4. **analysis_started** - нажал "Перейти к анализу"

5. **analysis_completed** - анализ завершён
   _→ ОТПРАВЛЯЕТСЯ УВЕДОМЛЕНИЕ В TELEGRAM_
   _Конверсия: завершили / сохранили_

6. **feathers_clicked** - нажал "Узнать пёрышки"
   _Конверсия: пёрышки / анализ_

7. **activities_clicked** - загрузились активности

8. **copy_clicked** - скопировал данные для нейронки
   _Конверсия: скопировали / пёрышки_

9. **telegram_clicked** - перешёл в ваш Telegram

### Вычисляемые метрики:

- **Уникальные посетители** - количество уникальных session ID
- **Всего сессий** - общее количество сессий
- **Конверсия в анализ** - % пользователей, завершивших анализ
- **Медиана ситуаций** - среднее количество ситуаций на пользователя
- **Медиана длины** - средняя длина одной ситуации в символах

---

## 🔍 Просмотр данных

### Через Telegram-бота (удобно):

```
/stats - покажет:
👥 Уникальных посетителей: 42
📱 Всего сессий: 45
✅ Завершили анализ: 28
📈 Конверсия: 66.7%
📝 Медиана ситуаций: 3.5
📏 Медиана длины: 145 символов
```

```
/funnel - покажет:
1️⃣ Зашли на сайт: 100
   ↓ 42.0%
2️⃣ Начали писать: 42
   ↓ 90.5%
3️⃣ Сохранили ситуацию: 38
   ↓ 73.7%
4️⃣ Отправили на анализ: 28
   ...
```

### Через API (для интеграций):

```bash
# Локально
curl "http://localhost:3000/api/analytics?key=your-analytics-secret-key"

# Production
curl "https://ваш-домен.vercel.app/api/analytics?key=your-analytics-secret-key"
```

### Через файл (для резервного копирования):

Данные хранятся в `analytics-data.json`:
```bash
cat "/Users/mihailmirosnicenko/Desktop/vibe projects/inversion/analytics-data.json"
```

---

## 🌐 Деплой на Production (Vercel)

### 1. Добавьте переменные окружения в Vercel:

Зайдите в **Settings → Environment Variables** вашего проекта и добавьте:

```
TELEGRAM_BOT_TOKEN = your-telegram-bot-token
TELEGRAM_CHAT_ID = your-chat-id
ANALYTICS_SECRET_KEY = your-analytics-secret-key
```

### 2. Задеплойте приложение:

```bash
cd "/Users/mihailmirosnicenko/Desktop/vibe projects/inversion"
vercel --prod
```

### 3. Запустите Telegram-бота на сервере:

Бот должен быть запущен на отдельном сервере (VPS, облако).

Создайте `.env` файл на сервере:
```env
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-chat-id
ANALYTICS_SECRET_KEY=your-analytics-secret-key
ANALYTICS_URL=https://ваш-проект.vercel.app/api/analytics
```

Запустите бота:
```bash
node telegram-bot.js
```

Рекомендуется использовать **PM2** для автозапуска:
```bash
npm install -g pm2
pm2 start telegram-bot.js --name inversion-bot
pm2 save
pm2 startup
```

---

## 📁 Структура файлов

```
inversion/
├── src/
│   ├── app/
│   │   ├── api/
│   │   │   └── analytics/
│   │   │       └── route.ts          # API endpoint для аналитики
│   │   └── page.tsx                   # Трекинг событий на главной странице
│   ├── components/
│   │   └── ResultsScreen.tsx          # Трекинг кликов (пёрышки, копировать, TG)
│   ├── lib/
│   │   ├── analytics.ts               # Утилиты для аналитики
│   │   └── telegram.ts                # Отправка уведомлений в TG
│   └── types/
│       └── analytics.ts               # TypeScript типы
├── telegram-bot.js                    # Telegram-бот для просмотра статистики
├── get-chat-id.js                     # Скрипт для получения Chat ID
├── analytics-data.json                # Файл с данными (создаётся автоматически)
├── .env.local                         # Переменные окружения (не коммитится)
├── ANALYTICS_SETUP.md                 # Подробная документация
├── QUICK_START_ANALYTICS.md           # Быстрый старт
└── README_ANALYTICS.md                # Этот файл
```

---

## 🛠 Troubleshooting

### Бот не отвечает на команды

**Решение:**
1. Проверьте что бот запущен: `node telegram-bot.js`
2. Проверьте `TELEGRAM_BOT_TOKEN` в `.env.local`
3. Перезапустите бота

### Не приходят уведомления о новых анализах

**Решение:**
1. Убедитесь что запущены **оба** сервиса (npm run dev + telegram-bot.js)
2. Проверьте `TELEGRAM_CHAT_ID` в `.env.local`
3. Проверьте логи в терминале на наличие ошибок

### Ошибка 401 при `/stats` или API запросах

**Решение:**
1. Проверьте `ANALYTICS_SECRET_KEY` в `.env.local`
2. Убедитесь что ключ одинаковый в обоих файлах (приложение и бот)

### analytics-data.json не создаётся

**Решение:**
1. Проверьте права на запись в директорию проекта
2. Убедитесь что Next.js приложение запущено
3. Откройте сайт в браузере чтобы создать первую сессию

### Статистика показывает нули

**Решение:**
1. Откройте http://localhost:3000
2. Пройдите весь путь пользователя (выпишите ситуации, отправьте на анализ)
3. Дождитесь завершения анализа
4. Отправьте боту `/stats`

---

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте [ANALYTICS_SETUP.md](./ANALYTICS_SETUP.md) для подробной документации
2. Посмотрите логи в терминалах
3. Проверьте файл `.env.local`

---

## 🎉 Готово!

Теперь у вас есть полноценная система аналитики с:
- ✅ Отслеживанием всех действий пользователей
- ✅ Уведомлениями в Telegram при новых анализах
- ✅ Ботом для просмотра статистики
- ✅ API для интеграций
- ✅ Вычислением медианных значений

**Следующие шаги:**
1. Запустите оба сервиса (см. раздел "Как запустить")
2. Протестируйте систему (пройдите весь путь пользователя)
3. Проверьте уведомление в Telegram
4. Попробуйте команды бота (/stats, /funnel)

Удачи с проектом! 🚀
